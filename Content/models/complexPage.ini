[model]
name = ComplexPage
label = {% if this.title %} {{ this.title }} {% else %} {{ this._id }} {% endif %}
hidden = no
protected = no

[fields.title]
type = string

[fields.starttext]
type = markdown

[fields.endtext]
type = markdown

[children]
model = complexPage-Block
order_by = _id

